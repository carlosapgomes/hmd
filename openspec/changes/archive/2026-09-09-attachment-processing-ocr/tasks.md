# Tasks: attachment-processing-ocr

Baseline: `80be50b` (change 09 arquivado; 852 testes verdes ×2 rodadas).

- [x] 1. Preflight: árvore limpa; `BASE_REF = c94c14c`; suíte completa verde ×1 (852 testes; ruff/format/mypy; validate --strict)
- [x] 2.1 Slice 001 — app `apps/attachments` (model `CaseAttachment` + migration 0001) + upload de anexos no intake + listagem NIR. Review: sem P1; P2s F1 (`original_filename` restaurado no D1/model/UI — causa: emenda anterior do pai) e F2 (input de anexos no form de reenvio corrigido) fechados pelo pai; F3 (conftest) aceito. 
- [x] 2.2 Slice 002 — extração híbrida (PyMuPDF local / `VISION_MODEL` externo com auditoria) + worker cluster `attachments` + trigger pós-anonimização. Review: 1 P1 (`ATTACHMENTS_RUN_TASKS_INLINE` sem fail-closed em prod.py) + 4 P2 fechados pelo pai (evento DISPATCHED só quando envio é possível via `ensure_vision_ready`; teste do próprio anexo deletado no meio; `#Q_CLUSTER_NAME=attachments`; worker-attachments no compose dev). Diferidos: duplicação do mapeamento SDK→LlmError, `select_for_update` por anexo. Desvios aceitos: kind "config" em LlmErrorKind (aditivo), flag inline explícita em test.py, 3 eventos canônicos já criados
- [x] 2.3 Slice 003 — anonimização alinhada ao caso + verificação LLM de patient-match (tokens apenas; prompt seed 29º). Review: sem P0/P1 (semeadura confirmada por leitura); 3 P2s de teste fechados pelo pai (schema-inválido → failed; atomicidade da persistência por injeção; assert de substituição do placeholder). Desvios aceitos: test_tasks.py atualizado (estado final muda p/ processed), mensagem única user em vez de system+user (registrar no arquivamento), analyzer fake (padrão da suíte)
- [x] 3.1 Slice 004 — cards de anexo na decisão médica (presenter re-identificado; mismatch = alerta). Review: sem P0/P1; 2 P2s fechados pelo pai (badge defensiva "unknown" p/ processed sem match; status_label no header do card + teste de página dos estados). Desvios aceitos: badge "pendente", cards sem corpo p/ não-processados, fallback ao mapa do caso só com mapa de anexo vazio
- [x] 4.1 Slice 005 — ciência do NIR remove anexos (integração closure + delta MODIFIED case-closure). Review: sem P0/P1 (conjunto D2 intacto, 8 testes do 09 preservados); P2 F1 fechado (asserts pré-ack de conteúdo das rows); F2 (bordas sem teste dedicado) aceito como nota
- [x] 5.1 Gate final: `uv run ruff check . && uv run ruff format --check . && uv run mypy . && uv run pytest` + `openspec validate attachment-processing-ocr --strict`; **940 testes ×2 rodadas** (reprodutibilidade na mesma DB reusada), ruff/format/mypy limpos, validate --strict ok
- [x] 5.2 Atualizar `PROJECT_CONTEXT.md` (estado pós-change; specs `attachments`/`case-closure` MODIFIED) e preparar arquivamento
